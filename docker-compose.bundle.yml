version: '3.8'

services:
  redis:
    image: ragedisplay/nexuswaf-redis:demo
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - waf-net

  signaturedb:
    image: ragedisplay/nexuswaf-signaturedb:latest
    ports:
      - "8082:8082"
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
    networks:
      - waf-net

  analyzer:
    image: ragedisplay/nexuswaf-analyzer:latest   
    ports:
      - "8083:8083"
    networks:
      - waf-net

  wafproxy:
    image: ragedisplay/nexuswaf-proxy:latest
    ports:
      - "${WAF_PORT:-8081}:${WAF_PORT:-8081}"
    environment:
      - TARGET_URL=http://192.168.200.50:7000
      - REDIS_ADDR=redis:6379
      - ANALYZER_URL=http://analyzer:8083/analyze
      - PORT=${WAF_PORT:-8081}
    depends_on:
      - redis
      - analyzer
    networks:
      - waf-net

  waf-admin:
    image: ragedisplay/nexuswaf-admin:latest  
    ports:
      - "8080:8080"
    environment:
      - REDIS_ADDR=redis:6379
      - WAFPROXY_URL=http://wafproxy:8081
    depends_on:
      - redis
      - wafproxy
    networks:
      - waf-net

networks:
  waf-net:

volumes:
  redis_data: